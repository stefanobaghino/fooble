Synthetic recipe pages that mirror the parts of fooby.ch's markup the extractor
reads: the canonical link, the schema.org JSON-LD blocks, the tag links, the
ingredient list and the portion-calculator attribute. Recipes, ids and URLs are
made up. `1002.html` deliberately carries a raw newline inside a JSON-LD string,
which fooby emits on some pages.
